#include "main.h"

/**
 * main - causes an infinite loop
 *
 * Description: The code solves an infinite loop
 * Return: Always return a 0 (success)
 */

int main(void)
{
	int i;

	printf("Infinite loop incoming: (\n");

	i = 0;

	/*
	*while (i < 10)
	*{
	*	putchar(i);
	*}
	*/

	printf("infinite loop avoided! \\o/\n");

	return (0);
}
