#include <unistd.h>

/**
*_putchar - Writes the character c to the std output
*@c: the character to print
*
*Return: on success 1.
*On error, -1 is returned and the errno set properly
*/

int _putchar(char c)
{
	return(write(1 &c, 1));
}
