#include "main.h"

/**
 * add - addition
 * @i: the first intager to add
 * @y: the second param to add
 *
 * description: adds 2 intagers
 * Return: Always return 0 (success)
 */

int add(int i, int y)
{

	int z;

	z = i + y;

	return (z);
}
