#include <stdio.h>
/**
 * main - Uses printf()
 *
 * Description: This program prints only one line using the printf() function
 * Return: Always returns 0 (success)
 */
int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,\n");
	return (0);
}
